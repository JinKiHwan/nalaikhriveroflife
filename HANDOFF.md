# 날라흐 생명수 교회 홈페이지 작업 인수인계

최종 갱신일: 2026-08-23
작업 경로: `/Users/wb/workspace/nalaikhriveroflife`

## 1. 프로젝트 개요

- Nuxt 4 + Vue 3 + TypeScript 프로젝트
- Firebase 인증 및 Firestore 사용
- 공통 기본 레이아웃: `layouts/default.vue`
- SCSS 디자인 토큰: `assets/scss/variables.scss`
- 전역 스타일: `assets/scss/main.scss`
- 기본 콘텐츠 최대 너비: `1380px`
- 이화여대 홈페이지를 참고한 전체 폭 헤더와 편집형 콘텐츠 구조의 교회 커뮤니티 사이트

## 2. 현재 Git 작업 상태

아래 파일에 커밋되지 않은 변경이 있다. 다음 작업자는 기존 변경을 덮어쓰거나 초기화하지 말고 이어서 작업해야 한다.

- `layouts/default.vue`
- `pages/index.vue`
- `assets/scss/variables.scss`

`git reset --hard`, `git checkout -- <file>` 등 기존 작업을 삭제하는 명령은 사용하지 않는다.

## 3. 공통 레이아웃 변경 사항

### 콘텐츠 폭과 배경

- 홈과 일반 페이지 콘텐츠는 최대 `1380px` 안에서 중앙 정렬한다.
- 배경 이미지는 메인 콘텐츠 영역 전체 너비로 표시한다.
- 배경은 상단 헤더 아래에서 시작한다.
- 데스크톱 배경 높이: `565px`
- 모바일 배경 높이: `480px`
- 배경 하단은 기본 배경색으로 자연스럽게 페이드된다.
- 페이지별 배경 연결:
  - 홈: `bg_03.webp`
  - 이번주 말씀: `bg_01.webp`
  - 공지사항: `bg_02.webp`
  - QT 묵상: `bg_04.webp`
  - 성경 통독: `bg_05.webp`
  - 교사방: `bg_06.webp`
  - 관리자: `bg_07.webp`

### 스크롤 안정화

- 페이지 이동 시 스크롤바가 생겼다 사라지며 화면 폭이 흔들리지 않도록 전역 세로 스크롤 공간을 고정했다.
- 관련 스타일은 `assets/scss/main.scss`의 `html` 규칙에서 확인한다.

### 상단 헤더

- 기존 고정 사이드바를 완전히 제거했다.
- 데스크톱은 로고, 중앙 가로 메뉴, 우측 언어 선택으로 구성한다.
- 헤더는 모든 페이지에서 `fixed`로 화면 상단을 따라온다.
- 홈에서는 처음에 메인 비주얼 위에 투명하게 겹치고, 스크롤하면 흰색 고정 헤더로 전환된다.
- 일반 페이지에서는 처음부터 흰색 고정 헤더를 사용하고 콘텐츠에 헤더 높이만큼 상단 여백을 둔다.
- `1100px` 이하에서는 가로 메뉴를 숨기고 전체 화면 드롭다운 메뉴로 전환한다.
- 모바일 메뉴 안에서 로그아웃할 수 있다.
- 모든 페이지 하단에 공통 교회 정보 푸터를 표시한다.

## 4. 홈 화면 리디자인

`pages/index.vue`는 이화여대 홈페이지의 편집형 정보 구조를 참고해 교회 사이트에 맞는 밀도로 전면 재구성했다.

> 한 화면의 큰 카드 모음이 아닌, 사진과 정보가 리듬 있게 이어지는 교회 소식지형 홈

주요 요소:

- 데스크톱 약 `580px`, 모바일 `500px` 높이의 말씀 중심 히어로
- 교회 십자가 사진 위에 텍스트 가독성을 위한 방향성 그라데이션 적용
- 히어로 하단에 겹쳐지는 얇고 정돈된 4열 바로가기 내비게이션
- 사진 중심 4열 교회 소식 섹션
- 짙은 청록색 배경의 최근 설교 집중 섹션
- 밀도 높은 2열 공지사항 목록
- 따뜻한 베이지 배경의 QT 묵상·성경 통독 2분할 섹션
- 짙은 네이비 배경의 예배 안내 섹션
- 콘텐츠 최대 폭 `1380px` 및 전체 폭 배경의 공통 원칙 유지
- 태블릿 2열, 모바일 1열 반응형 구성
- `prefers-reduced-motion` 대응
- 모바일 가로 넘침 방지를 위해 `.site-main`, `.home-page`에 `min-width: 0` 적용

기존 링크와 정보 구조는 유지한다.

## 5. 다국어 구조

### 핵심 파일

- `composables/useLanguage.ts`

### 동작 방식

- 지원 언어: 한국어 `ko`, 몽골어 `mn`
- `useCookie('site-language')`로 선택 언어를 저장한다.
- 새로고침과 페이지 이동 후에도 선택값이 유지된다.
- `<html lang>` 값도 선택 언어에 맞게 변경한다.
- 각 컴포넌트에서 다음과 같이 사용한다.

```ts
const { language, t } = useLanguage()
```

```vue
<h1>{{ t('sermons.title') }}</h1>
```

### 현재 번역 적용 범위

- 교회명과 지역명
- 사이드바 메뉴
- 역할명과 로그아웃 문구
- 모바일 상단 교회명
- 홈의 말씀, 바로가기, 최근 설교, 교회 소식, 예배 안내
- 말씀 목록 페이지의 주요 제목, 설명, 빈 상태, 기본 버튼
- 공지 목록 페이지의 주요 제목, 설명, 빈 상태, 기본 버튼
- QT 페이지의 주요 제목, 설명, 빈 상태, 댓글 영역
- 성경 통독 페이지의 주요 제목, 통계 라벨, 안내 및 빈 상태

사용자가 직접 작성한 설교, 공지, QT 본문과 댓글은 자동 번역하지 않고 작성된 원문을 그대로 표시한다.

## 6. 아직 번역이 완전하지 않은 영역

다음 작업자가 우선 확인할 부분이다.

- 말씀 등록 관리자 폼의 세부 라벨과 오류/확인 메시지
- 공지 등록 관리자 폼의 세부 라벨과 오류/확인 메시지
- QT 등록 폼의 세부 라벨과 오류/확인 메시지
- 성경 통독 입력 폼, 피드 문장, 성경 이름 목록
- 말씀/공지/교사방 상세 페이지의 정적 UI 문구
- 교사방 전체 UI
- 관리자 전체 UI
- 별도 인증 레이아웃을 사용하는 로그인 페이지
- 날짜 표기 방식의 언어별 현지화

번역을 추가할 때 문자열을 페이지 안에 직접 분산시키지 말고 `useLanguage.ts`의 메시지 키를 확장하는 방식을 유지한다.

## 7. 디자인 작업 시 유지할 기준

- 콘텐츠 최대 너비는 `1380px`을 유지한다.
- 배경은 전체 너비, 실제 콘텐츠는 중앙 컨테이너 방식으로 구분한다.
- 브랜드 컬러:
  - 리버 틸: `#0f766e`
  - 샌드 골드: `#b97836`
  - 딥 네이비: `#18243a`
- 브랜드 포인트는 그라데이션 없이 매트한 리버 틸 단색으로 사용한다.
- 과도한 글래스 효과와 강한 그림자를 피한다.
- 모바일에서는 배경 장식을 단순화한다.
- 사용자 작성 데이터와 Firebase 동작은 디자인 수정 과정에서 변경하지 않는다.
- 현재 로컬 배경 이미지 `public/images/bg_01.webp`~`bg_07.webp`를 사용한다.

## 8. 확인 완료 항목

- `npm run build` 성공
- SCSS 및 TypeScript 컴파일 성공
- 홈 데스크톱 4열 바로가기 카드 확인
- 홈 모바일 1열 카드와 가로 넘침 없음 확인
- 사이드바 제거 및 데스크톱 중앙 가로 메뉴 확인
- 모바일 전체 화면 메뉴 열기·닫기 확인
- 일반 페이지의 흰색 고정 헤더와 `1380px` 중앙 정렬 확인
- 홈 스크롤 시 투명 헤더가 흰색 고정 헤더로 전환되는 동작 확인
- 한국어 → 몽골어 선택 시 다음 항목 전환 확인:
  - 상단 및 모바일 메뉴
  - 교회명
  - 홈 말씀
  - 바로가기 카드
  - 홈 전체 편집형 콘텐츠 섹션
- 새로고침 후 몽골어 선택 유지 확인
- 몽골어 상태에서 `/sermons` 페이지 제목, 설명, 빈 상태 전환 확인
- 브라우저 콘솔 오류 없음

## 9. 권장 다음 작업

1. `useLanguage.ts`의 번역 키를 확장해 나머지 페이지의 정적 UI 번역을 완료한다.
2. 몽골어 문구는 실제 사용자가 검수할 수 있도록 별도 목록으로 정리한다.
3. 관리자 권한으로 등록 폼과 삭제 확인 창을 실제 테스트한다.
4. 태블릿 너비에서 긴 몽골어 메뉴명과 카드 제목 줄바꿈을 점검한다.
5. Firebase 데이터가 있는 환경에서 목록/상세 레이아웃을 다시 확인한다.
6. 완료 후 변경 파일을 검토하고 하나의 기능 단위 커밋으로 정리한다.

## 10. 자주 사용하는 확인 명령

```bash
npm run dev
npm run build
git status --short
git diff --check
```

개발 서버 기본 주소: `http://localhost:3000/`
